# ============================================
# 🚀 PIPELINE COMPLETA - MOTO MONITORAMENTO (CI + CD)
# ============================================

trigger:
  branches:
    include:
      - main
      - master

pool:
  vmImage: 'ubuntu-latest'

variables:
  buildConfiguration: 'Release'

stages:
  # ============================================
  # 🧱 CI - Build + Testes + Artefatos (dotnet publish)
  # ============================================
  - stage: CI
    displayName: "Integração Contínua (Build + Testes)"
    jobs:
      - job: BuildAndTest
        displayName: "Build e Testes Automáticos"
        steps:
          - checkout: self
            displayName: "📦 Checkout do repositório"

          - task: UseDotNet@2
            inputs:
              packageType: "sdk"
              version: "9.0.x"
            displayName: "🔧 Instalar .NET 9 SDK"

          - task: DockerInstaller@0
            displayName: "🐳 Instalar Docker"

          - script: |
              echo "📦 Restaurando dependências..."
              dotnet restore MotoMonitoramento.sln
            displayName: "📦 Restaurar dependências"

          - script: |
              echo "🏗️ Compilando aplicação..."
              dotnet build MotoMonitoramento.sln --configuration $(buildConfiguration) --no-restore
            displayName: "⚙️ Compilar aplicação"

          - script: |
              mkdir -p $(Build.ArtifactStagingDirectory)/TestResults
              mkdir -p $(Build.ArtifactStagingDirectory)/App
            displayName: "📂 Preparar diretórios para artefatos e testes"

          - script: |
              echo "🧪 Executando testes Unitários..."
              dotnet test tests/MotoMonitoramento.Tests.Unit/MotoMonitoramento.Tests.Unit.csproj \
                --no-build \
                --logger "trx;LogFileName=$(Build.ArtifactStagingDirectory)/TestResults/UnitTests.trx"

              echo "🧪 Executando testes de Integração..."
              dotnet test tests/MotoMonitoramento.Tests.Integration/MotoMonitoramento.Tests.Integration.csproj \
                --no-build \
                --logger "trx;LogFileName=$(Build.ArtifactStagingDirectory)/TestResults/IntegrationTests.trx"
            displayName: "🧪 Executar testes Unitários e Integração"

                    
            # Verificar se os arquivos TRX foram realmente criados (debug)
          - script: |
              echo "🔍 Conteúdo de TestResults:"
              ls -la $(Build.ArtifactStagingDirectory)/TestResults
            displayName: "🔍 Verificar arquivos de teste"

          # Publicar resultados de teste
          - task: PublishTestResults@2
            inputs:
              testResultsFormat: "VSTest"
              testResultsFiles: "$(Build.ArtifactStagingDirectory)/TestResults/*.trx"
              failTaskOnFailedTests: true
            displayName: "📊 Publicar resultados dos testes"

          - script: |
              echo "📂 Publicando aplicação para ArtifactStagingDirectory..."
              dotnet publish MotoMonitoramento.sln -c $(buildConfiguration) -o $(Build.ArtifactStagingDirectory)/App
            displayName: "📂 dotnet publish para artifact"

          - task: PublishBuildArtifacts@1
            inputs:
              PathtoPublish: "$(Build.ArtifactStagingDirectory)"
              ArtifactName: "drop"
              publishLocation: "Container"
            displayName: "📦 Publicar artefatos"

          - task: AzureCLI@2
            inputs:
              azureSubscription: "qmove-azure-connection"
              scriptType: "bash"
              scriptLocation: "inlineScript"
              inlineScript: |
                echo "🐳 Build e push das imagens para o ACR..."
                chmod +x ./build.sh
                ./build.sh
            displayName: "🐳 Build e push Docker"

  # ============================================
  # ☁️ CD - Deploy automático no Azure
  # ============================================
  - stage: CD
    displayName: "Entrega Contínua (Deploy no Azure)"
    dependsOn: CI
    condition: succeeded()
    jobs:
      - job: Deploy
        displayName: "🚀 Executar Deploy no Azure"
        steps:
          - checkout: self
            displayName: "📦 Checkout do repositório"

          - task: AzureCLI@2
            inputs:
              azureSubscription: "qmove-azure-connection"
              scriptType: "bash"
              scriptLocation: "inlineScript"
              inlineScript: |
                echo "☁️ Executando deploy no Azure..."
                chmod +x ./deploy.sh
                ./deploy.sh
            displayName: "☁️ Deploy via Azure CLI"
